This application creates a series of prompts for generative AI (e.g. Stable Diffusion or Midjourneuy). It takes as input a template and a set of parameters 
Then it create one prompt for each combination
